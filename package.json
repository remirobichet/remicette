{
  "name": "remicette",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @remicette/web dev",
    "dev:api": "pnpm --filter @remicette/api start:dev",
    "build": "pnpm -r build"
  }
}
